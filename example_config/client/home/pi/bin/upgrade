#!/bin/sh

if [ -f /etc/cron.hourly/dietpi ] ; then
	mv /etc/cron.hourly/dietpi /etc/cron.hourly/.dietpi
fi

if [ -f /etc/bashrc.d/dietpi.bash ] ; then
	mv /etc/bashrc.d/dietpi.bash /etc/bashrc.d/.dietpi.bash
fi
